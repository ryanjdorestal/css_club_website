# assets/refs — moved out of the repo (run 8)

The reference material the site was matched against (about 50 MB of screenshots and
clippings) now lives **outside the repository** at `~/Desktop/jjay_css_refs/` on Ryan's
machine, so a clone stays small. Folders, unchanged:

| Folder | What |
|---|---|
| `jj_inspo/` | the John Jay inspiration boards (`jj_01`…) — the look the public site matches |
| `rhecwb/` | rhecweb screenshots — composition benchmark only, never colours |
| `type/` | type studies from run 3 (`scripts/collect_type_refs.mjs` writes here) |
| `hound/` | Bloodhound / Cyberhound references |
| `club/`, `old-site/`, `supavoxel/` | club photos, the old site, the cube's voxel study |

Docs refer to them as `../jjay_css_refs/<folder>/` (a sibling of the repo). If you need them
and don't have the folder, ask Ryan (ryanjdorestal@gmail.com); nothing in the build depends on
them — they are for matching the design by eye.
