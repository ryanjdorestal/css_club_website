# 24 — Type notes (run 3). Three lines per cell: letterforms · micro-system · what I reproduce where.

- **T01 UTOPIA TOKYO** — Squared caps w/ stencil cuts (barless A, sheared U/T joins), solid word + outline word on ONE line; body in a plain grotesk on the red field. Micro: coord rail `35.6762° N / 139.6503° E`, `VERSION: 2.0.0-RC.1` corner, `[ >_EXECUTE_CREATION ]` bracket CTA on a chamfered dark stub, hairline row-grid behind MASKED./MARKED./WATCHED., tick-ruler meter, diamond sigil, black chamfered page frame. Reproduce: Home H1 solid+outline pair + coord rail + VERSION corner + bracket CTA; the row-grid behind the About-spread headline.
- **T02 LAST SURVIVOR** — Ultra-extended squared display (flat curves, deep notches), cropped by the right edge; red 1px wireframe vehicle drawn THROUGH the letters. Micro: `18 · 02 · 86` top rail, vertical barcode + sigil top-right. Reproduce: Events H1 EdgeCrop in Michroma + cube wireframe through the Events band H2; index rail top of poster bands.
- **T03 permitify** — Pixel/dot-matrix caps headline (AI-POWERED PLAN REVIEW), `[1][2][3]` mono index list, giant `1 /5` pixel pagination. Micro: chamfered black panel corner, pixel-eye sigil w/ corner ticks, red field + registration marks. Reproduce: Silkscreen poster eyebrows + big counters; [n] list prefixes everywhere; Eye sigil; chamfered panel on 404/styleguide.
- **T04 CYBER BRUTALISM** — The full register on WHITE: heavy condensed display, mono protocol line in accent, body mono 55%. Micro: `/01…/07`, `//SCN_01`, `X_36.1749` coord chip, `> RENDERING 83%`, `+` registration corners, ↗ on every card, hatch strip, SYSTEM STATUS segmented meters, `ALL SYSTEMS OPERATIONAL` chip, nav `SYS.TIME` cell, bottom status bar `CONNECTION SECURE / > ACCESS GRANTED_ / SCN: 0007 / NODE: CYBR_01`, sigil pictogram row w/ labels. Reproduce: EVERYTHING on paper bands + the global status bar + nav SYS.TIME + sigil row treatment for About/what-we-do.
- **T05 bunker card** — Photo card: inset frame w/ tick marks outside corners, diagonal bottom-right cut with an ORANGE STUB BAR in the notch, `001` index in accent, title body-grotesk, "Know more +" w/ attached square end-cap tab. Reproduce: Chamfer+stub on TicketCard; Button Block end-cap anatomy; PhotoFrame registration ticks.
- **T06 VITALITY** — Half-solid/half-outline single word (VIT solid white / ALITY outlined teal) centered in a wireframe radar cube w/ vertex dots + corner brackets. Micro: `GENERATING MASK` display + `65%` accent, segmented hatch progress bar, `HASH: 0xFE85A1 · PROCESSING DATA CHUNKS · BLOCK: Q-855` rail, bordered numeral readouts, red grid at 6%. Reproduce: SplitFill on GROWTH./BUILT.; Meter anatomy (segmented + % + hash rail); Readout HUD boxes; brackets around the cube frame.
- **T07 INVASION** — Stencil caps w/ glyph substitution (V→U-form, barless A), giant thin katakana strokes as background sigil at outline weight; pixel-art masks floating on a dot grid. Micro: same chamfered page frame + coords + VERSION + `0/14` counter; `>_TAKE_ME_HOME` bracket CTA in a red panel. Reproduce: background outline sigil (CSSKufic at 40vw outline) on Join; bracket CTA hover fill; page-frame brackets on 404.
- **T08 MONDRAM** — Logotype letters as stencil rectangles with gaps (sigil-letterforms) hung as cards; boarding-pass system: label rails in black w/ white knockout (`BOARDING PASS / BD / UF D-3848` rotated), giant condensed mono values `B12 / 4.20ᴾᴹ / FEB.01.2016 / 10C / 6.30ᴾᴹ`, barcode, `REG. A/02` dot-matrix card, GATE `C-4`, ↗ arrow panel. Reproduce: TicketCard v2 exactly — rotated tab rail, giant mono values w/ raised units, barcode; footer tagline rail.
- **T09 N1–N5** — 1-bit dither raster eye w/ red pixel accent; `N1 N2 N3 N4 N5` red mono rail top+bottom; tiny tracked-caps legal line; `STAY ALERT. STAY SAFE. FOLLOW PROTOCOL.` protocol sentence. Reproduce: Cyberhounds protocol line + HoundPixel dither treatment + N-rail as //S25 //F24 semester rails.
- **T10 Trouble (jj_07)** — Square-Kufic wordmark: Latin letters as right-angle maze strokes, uniform width, no curves, on a 5×5-ish grid, framed panel w/ caption tab. Reproduce: CSSKufic sigil (footer ornament, Join watermark, 404) traced by hand on a grid.
- **T11 phone tickets (jj_10)** — Chamfered stub cards, side tab w/ rotated label, split navy/coral tones, `CMD™ Model S21Y4` ID copy, `Performace 0.719` big-number field, barcode strip. Reproduce: TicketCard Tab + chamfer + ID grammar (EVT-S25-01 as "model numbers").
- **T12 drone HUD** — `_type/_status/_cargo/_operator` underscore key rail: 8-9px caps grey key ABOVE 11px white value; bottom telemetry strip split into bordered cells w/ ↗/↘ glyphs, GPS coords as decoration; tab buttons as bordered mono caps. Reproduce: SpecSheet two-tier key/value + the StatusBar cell anatomy + form field _labels.

Secondary skim (S01-S14 collected): S01 corner-bracket crop marks + `104 : 0768` Silkscreen counter → fin line; S09 mono card grammar (tag chips/Phase stub) → StatusChip; S11 energrid `PL-HV-119` IDs + state dots → OS queue IDs; S12 3-corner poster rails → PosterCard; S14 TRA cropped `052` numerals → stat numerals w/ EdgeCrop.
