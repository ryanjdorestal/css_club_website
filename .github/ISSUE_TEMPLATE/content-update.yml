name: Content update
description: A fix to copy, a link, an event, a resource, a photo
labels: [content]
body:
  - type: dropdown
    attributes: { label: Where, options: [Home, Events, Projects, Cyberhounds, About, Resources, News, Join] }
  - type: textarea
    attributes: { label: Current → wanted, description: "Quote the current text/link, then the change" }
    validations: { required: true }
  - type: markdown
    attributes: { value: "Board members can usually make this change themselves in CSS OS (/os) without a PR." }
