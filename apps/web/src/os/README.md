# os

The board platform (/os). `session.tsx` = who is signed in; `ui/` = OsPage/OsTable/OsForm/Upload/useOs; one `Os<Module>.tsx` per rail entry (see `OsLayout.tsx::OS_MODULES`). Every page ends with a "What is not here" block.
