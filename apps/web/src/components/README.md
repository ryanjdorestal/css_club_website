# components

Page-level pieces (Nav, Footer, PageHero, Band, FinLine, MapCard, Pennant…) + `cards/` (the card family), `frame/` (brackets, chamfer, registration), `type/` (display-type treatments + the Label grammar). Tokens only — never hex.
