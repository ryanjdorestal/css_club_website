# 41 — Run 10 log (type v5 · the OS actually works)

Start 2026-09-21 13:45 (the stale `.git/index.lock` + `HEAD.lock` removed first; the expanded prompt
committed). Budget 220 min (+10 report). Brief: `40_RUN10_PROMPT.md`.

## Checkpoints

| Time  | Min | Shipped                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----- | --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 13:59 | 14  | **A done.** `fonts/fonts.css` (22 `@font-face`, every family self-hosted incl. Space Grotesk / JetBrains Mono / VT323 copied out of the packages, OFL LICENSE each), tokens v5, type.css re-fit, the drawn alphabet + the geometric face deleted, 4 pages back on text treatments, TYPE v5 specimen, per-realm `font_audit` green on 13 routes, preloads, `@fontsource` uninstalled. `grep` gate 0, build gate 0.             |
| 14:08 | 23  | Matrix (`42_OS_MATRIX.md`, before: ✅ 47 · ⚠️ 29 · ❌ 60). API core: error envelope, client_id idempotency, 409 conflicts, archive/unarchive/duplicate on every entity, atomic Tier-1 writes + `make restore`, uploads by magic bytes, **workshops** entity, terms/resources/members/projects gaps; 125 pytest.                                                                                                               |
| 14:20 | 35  | OS shared infra: `useOs.act` (envelope + client_id + expected_updated_at), toasts with UNDO, typed confirms, `OsForm` (rules, validation, jump links, ⌘↵, draft restore, SAVING…), `OsTable` (row menus, keyboard, `ListTools`), focus-trapped `Panel`, inline-edit `KeyVal`, `Empty`; Posts/Events/Workshops on `useEntity`; Projects/Board/Resources/Members actions.                                                       |
| 15:46 | 121 | **`board_sim.mjs` 25/25.** Found and fixed on the way: a Tier-1 write race (the table file vanished for a moment between two `os.replace` calls → a concurrent reader re-seeded it; now copy `.bak` + a lock), Join reading the static Discord link, /about hiding an empty current term, category order not honoured by the public read, `group_sort` dropped by the model, epoch-second timestamps.                         |
| 17:03 | 198 | **`board_break.mjs` 16/16.** Found and fixed: the public site ignored the maintenance banner and the feature flags (now honoured); two client_ids per double-click (one per form session + an in-flight guard); the local role cached per session (read fresh → a cleared session fails the next save); drafts removed before the request (now after success); the panel focusing the close button (first field, next frame). |
| 17:20 | 215 | Docs (matrix after, CONTRIBUTING "adding an entity", HANDOFF list, DESIGN v5, FONTS.md), CI `make sim` + `make break`, report.                                                                                                                                                                                                                                                                                                |

## Decisions made without asking

| #   | Decision                                                                                                                                                                                                                          | Why                                                                                                                                                                                                                                      |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | The three families the run keeps (Space Grotesk, JetBrains Mono, VT323) are copied out of their npm packages into `src/fonts/<family>/` with their LICENSE and the packages uninstalled                                           | the grep gate forbids `fontsource` anywhere under `apps/web/src`; self-hosting everything is the same rule for all faces                                                                                                                 |
| 2   | Home hero dek = the first two sentences of the old-site paragraph (`heroDekShort`)                                                                                                                                                | the approved mock sets three caps lines; the full paragraph in caps was six                                                                                                                                                              |
| 3   | Silkscreen 700 = the OS face; VT323 stays for rings + ticker digits                                                                                                                                                               | `qa/loops/run10/os_face.png` — chunkier and cleaner at 72 px against T03                                                                                                                                                                 |
| 4   | Orbitron ships unused                                                                                                                                                                                                             | no poster band read light in Turret Road 800                                                                                                                                                                                             |
| 5   | `font_audit`: labels at exactly 11 px are Martian Mono (§3.3) so the micro bucket allows both monos; `/styleguide` alone may show Silkscreen ≥ 28 px (the specimen); Michroma is allowed on `/os/login` (the public nav logotype) | the rules as written, with the two places they meet                                                                                                                                                                                      |
| 6   | Workshops are their own page (`/os/workshops`, rail sigil Chevrons) rather than a tab in Events                                                                                                                                   | a series with numbered sessions is a different shape from an event; the public Events page still carries the section                                                                                                                     |
| 7   | Archive = `status: archived` (remembering `archived_from`) on lifecycle tables, an `archived` flag elsewhere; unarchive restores the previous status                                                                              | one generic action for every entity, reversible                                                                                                                                                                                          |
| 8   | Delete stays where the brief allows it (drafts, links, terms without officers/records, members); an officer who filed a handoff is archived with a 409 that says so                                                               | history is the point of the spine                                                                                                                                                                                                        |
| 9   | `feature` allows three; the fourth 409 names the three                                                                                                                                                                            | §7 Projects                                                                                                                                                                                                                              |
| 10  | Resubmit = the public form finds the author's `changes_requested` row with the same slug and updates it (history appended, reviewer note returned)                                                                                | "same slug; history preserved"                                                                                                                                                                                                           |
| 11  | Tier-1 concurrency: one process-wide `RLock` around every read-modify-write and a _copy_ (not a move) to `.bak`                                                                                                                   | the sim lost an event when a reader saw the table file missing between two renames                                                                                                                                                       |
| 12  | Reorders and renames are one write (`patch_many`)                                                                                                                                                                                 | 26 sequential patches raced the public read in step 10                                                                                                                                                                                   |
| 13  | The browser outbox (`localStorage os-outbox`) replays on the next OS load and from a chip in the shell; a 4xx on replay counts as delivered (the server decided; the audit has it)                                                | §9 case 07 without a service worker                                                                                                                                                                                                      |
| 14  | A 401 mid-write marks the session expired (`markSessionExpired`) so the bounce reads SESSION_EXPIRED, and the draft is removed only after a successful save                                                                       | §9 case 10                                                                                                                                                                                                                               |
| 15  | `board_sim` and `board_break` remove only the spine files they created (`git ls-files --others content/inheritance` — every untracked file, ignored or not) — never a committed record                                            | the first version deleted the seed decision record (restored from git); a second version listed `--ignored` only, which is nothing under a folder with no ignore rule, so the sim's handoffs stayed and broke the smoke's validator step |
| 16  | The sim's terms step edits the seeded F26 (dates + set current) when it exists and creates it otherwise                                                                                                                           | the committed `terms.json` already lists F26                                                                                                                                                                                             |
| 17  | The existing functional smoke was adapted to the new form markup (`getByLabel`), a real date field and the three-featured rule — not weakened                                                                                     | its assertions still check the public site                                                                                                                                                                                               |
| 18  | Five ⚠️ stay in the matrix and are named in the pages' "what is not here" blocks (record delete from the UI, ticker reorder, audit actor/date filters, per-item replay)                                                           | time; each is one line honest rather than a half control                                                                                                                                                                                 |
| 19  | The break commit had swept in ten `data/*.local.json.bak` copies and five sim-generated `historian-*.md` handoffs; the report commit untracks them and `.gitignore` gains `data/*.local.json.bak`                                 | store copies and sim output are not content                                                                                                                                                                                              |

## Left out (named, not hidden)

- `scripts/validate_data.py` was not extended to cross-check referenced ids / image paths (11 schemas still pass in `make check`).
- Inheritance `supersedes` picker and owners multi-select stay text fields (validated server-side).
- Site settings: the typed per-key controls (string-list add/remove/reorder) stay the JSON control with the banner toggle.
- Audit: no actor/date-range filters, no per-item replay; System: no `RUN SNAPSHOT` button (the snapshot is `make snapshot` / the workflow).
- Lighthouse was not re-run this run (run 9: 92 / 94 on the preview build; only fonts changed since).

## Close (17:25 — 220 of 220 min)

`make check` green · `make a11y` 0/0 · OS gate 12/12 · functional smoke 9/9 (after the form-markup adaptation) · `board_sim` 25/25 · `board_break` 16/16 · pytest 125 · vitest as before. Local tables, inbox and sim spine files reset; dev servers left running; nothing pushed.
