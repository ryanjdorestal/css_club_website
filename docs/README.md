# docs

Living docs for the board and developers: RUNBOOK (red chips), HANDOFF (each term), DECISIONS (the why), LATER (deferred). `archive/` holds the planning history from the build runs (run numbers live only there).

Also: `SKILLS_ADOPTED.md` (every check, where it came from, findings before/after), `RELEASE.md` (the push checklist), `API.md` (generated — `make api-docs`).
