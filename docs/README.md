# docs

Living docs for the board and developers: `handoff/` (the next maintainer's package, start at
`handoff/00_START_HERE.md`), RUNBOOK (red chips), HANDOFF (the board's page), OWNERSHIP + TERM_CHECKLIST (the
board's two lists), HOSTING_LIMITS + ENVIRONMENT + REPO_SETTINGS (infra), CODE_STANDARDS (what review checks),
DECISIONS (the why), LATER (deferred). `archive/` holds the planning history and every run's prompt + log (run
numbers live only there).

Also: `SKILLS_ADOPTED.md` (every check, where it came from, findings before/after), `RELEASE.md` (the push
checklist), `API.md` (generated — `make api-docs`).
