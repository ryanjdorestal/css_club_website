# content

Markdown the site renders: `home.md`, `about.md`, `cyberhounds.md` (migrated copy), `news/<slug>.md` (one per published post — written by the snapshot), `handoffs/<term>.md` (filed handoffs). Frontmatter + `##` headings + paragraphs only.
